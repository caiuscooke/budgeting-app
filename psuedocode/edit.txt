type of trans: amount category date time

def edit_list


choice = input()
print ("what do you want to edit about transaction:product amount category date time")
make a list of trans_list ?

for iteam in trans_list 
if choice == itemin list:
print ("are you sure you want to edit {choice} y/n")
if "y"== : 
append choice in list 
else:
edit_list()


password
import hashlib
h = hashlib.new("SHA256")
or 
h =hashlib.SHA256()
h.update(b"hash_info.encode()")
print (h.digest())
print (h.hexdigest()) best way to do it 
EX
import hashlib
h = hashlib.new("SHA256")
pw = input()
h.update(b"pw.encode()")
print (h.hexdigest()) 